import React from "react";
import styles from "./Title.module.css";

export const Title = () => {
  return (
    <div>
       <h1 className={styles.title}>NASA moments</h1>
    </div>
  );
};

export default Title;